{
  "name": "@v-factor/server",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "dev": "nodemon --watch src --ext ts --exec 'npm-run-all build start'",
    "lint": "npm-run-all --parallel lint:src",
    "lint:src": "tslint --fix",
    "prebuild": "yarn clean",
    "start": "node dist",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "dependencies": {
    "express": "^4.16.4"
  },
  "private": true,
  "publishConfig": {
    "access": "public"
  }
}
