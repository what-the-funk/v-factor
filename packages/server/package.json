{
  "name": "@v-factor/server",
  "version": "1.0.0",
  "scripts": {
    "clean": "rimraf dist",
    "prebuild": "yarn clean",
    "lint": "tslint -p src --fix",
    "tsc": "tsc",
    "build": "npm-run-all clean lint tsc",
    "dev:start": "npm-run-all build start",
    "dev": "nodemon --watch src -e ts --exec npm-run-all dev:start",
    "start": "node .",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "dependencies": {
    "express": "^4.16.4",
    "jest": "^24.1.0",
    "peer": "^0.2.10"
  },
  "private": true,
  "publishConfig": {
    "access": "public"
  }
}
